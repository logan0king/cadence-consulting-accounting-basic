apiVersion: v1
kind: Service
metadata:
  name: cadence-accounting-service
  namespace: staging
  labels:
    app: cadence-accounting
    environment: staging
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: cadence-accounting

